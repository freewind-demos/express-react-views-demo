express-react-views demo
========================

Render react components on the server side.

```
npm install -g babel-cli
npm install
npm start
```

Then visit <http://localhost:3000> and you will see `Hello world!` on the page.